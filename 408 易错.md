- 41H=**4×16**+1
- 12H 前不要再加 "0x“ ！
- 高优先级进程抢占当前进程 P，P 进入**就绪态而非阻塞态**
- 算信道利用率注意是**单向传播时延**还是双向
- TLB/Cache 映射方式注意区分全相联和直接映射（有行号）
- 虚拟页号对应实页号注意不要对应错了！
- 混合索引中间址地址项存在磁盘/簇中而不是索引节点！
- **MTU 易错**
	- 除最后一片，其他数据部分是8B的倍数，如MTU=800B，首部20B，数据部分最多776B
	- **片偏移量是8的倍速**
	- 第一个字节&最后一个字节编号
- 浮点数的无穷大具体说明正无穷/负无穷
- "存入（c 语言）**数组**"，**下标-1 !!**
- int 数组元素占 4 字节，按字节编址的系统每隔一个元素，其地址应该**加 4**
- 磁头从 0 号磁道 → 399 号，移动 399，不是 400！
- ⚠ 在散列表中查找关键字的比较序列是**关键字**，不是其序号
- **大题中写某信号、寄存器编号要回头看题目中有没有给出**
- 大题中数组序号 i 注意从 0 开始！
- unsigned short → int：零扩展
- 根据算法访问页框缺页次数要算**初始为空时的缺页**
- 区别“第n个”和“从0开始的n号”
- 注意**序号问题**，比如地址/磁盘块大小=50，地址%磁盘块大小=12，此时地址应存在第51块，因为地址比前50块单元数大
- 算进程平均周转时间画甘特图，如果考虑切换时间直接算容易少加
- 算 Cache 总容量注意**单位**，块大小是 B，而标记阵列是 b
- 注意**全相联没有行号字段**，和直接映射区分
- ==分页存储管理== **没有**逻辑上扩充主存容量，解决的是可以不连续存放
  ==请求分页存储管理== 才扩充
- int 型`0xff`高位省略了0，实际上是正数