#### DS
---
1. 前后中缀表达式转换，表达式树
2. KMP
3. 哈希各种散列函数、处理冲突方法
4. 哈夫曼编码加权平均长度
5. LR/RL，红黑树（注意“叔”不是兄弟）
6. 折半查找树高，成功/失败查找长度，失败不算叶结点
7. AOE网
8. 二路归并
9. 算法代码(Floyd、Dijkstra、二路归并、逆拓扑、并查集两种优化)
10. 特殊数组存放方式
11. B树结点/关键字最少最多、插入删除，插入新节点读写磁盘数最多3h+1，结点内容
12. 线索二叉树构造与遍历
13. 有序表查找不成功的ASL
14. 给定关键字个数问最多有多少颗B树
15. 各种排序比较
- 归并一趟调用n/2l(向上取整)次merge，将前后相邻且长度为l的有序段两两合并，共需h-1=log2n(向上取整)趟

#### CO
---
1. 翻译过程
2. 乘法、Booth乘法器、除法
3. 溢出判断
4. 操作元件、状态元件，数据通路
5. 流水线冒险，高级流水线技术，性能指标，隔一条的运算类指令从哪里取
6. RISC/CISC
7. 多处理器
8. ISA
9. 计数单位
10. Cache 标记阵列，全相联和直接映射字段
11. 总线/IO端口寄存器传送的信息
12. RAID
13. 浮点数舍入
14. 控制信号
15. 逻辑表达式

- ![[Pasted image 20251214154853.png]]
- 存储单元包含若干存储元件，存一串二进制代码-存储字，长度为存储字长
- 字长：CPU内部用于整数运算数据通路的宽度，和字是不同的概念
  存储字长-存储单元二进制位数
  指令字长-指令字中二进制位数
- a.c--a.i--a.s--a.o(二进制文件)--可执行文件a(存在磁盘)
- 算数左移前后符号位不同则溢出

#### OS
---
1. 中断I/O流程，内核缓冲区
2. 设备分配回收
3. 页表物理地址&虚拟地址，页表自映射
4. 微内核
5. 内核态→用户态
6. 段长度
7. 编译-汇编-链接-装入
8. 操作系统引导
9. 设备独立性软件、设备驱动程序、设备控制器
10. PCB、索引节点
11. 进程通信
12. 死锁预防和死锁避免易混说法
13. 地址变换机构
14. 操作系统结构
15. 三种链接、三种装入

- **未被修改**的页面换出时，将其所在的**页框**挂到**空闲**页框链表
- 守护进程 kswapd 负责页面换入换出，内核的大部分页框不能回收，进程使用的页框大部分可以回收
- ==循环缓冲== 多缓冲
	- 循环队列，in指向第一个可输入数据的，out指向第一个可输出的
- ==缓冲池==
	- 

#### CN
---
1. 各种校验和
2. 移动IP
3. OSPF分组类型、BGP报文类型，BGP会话、路由
4. IPv6
5. SDN 网络体系
6. 编码
7. DNS 访问具体的域名服务器
8. 片偏移
9. UDP 数据报
10. 发送窗口大小相关题目
11. 链路传输时延、带宽、吞吐量
12. 滑动窗口，信道利用率
13. IP地址
14. PPP
15. 交换机
16. TCP持续


- OSI 数据链路层-可靠，流量和差错控制；网络层-虚电路和数据报；传输层-仅有连接可靠；会话层-检查点机制，可靠会话，断点下载；表示层、**应用**层
  TCP/IP 网络接口层=物理+数据链路；网际层仅IP；传输层两种
- 基带传输：原始电信号；宽带传输：调制基带信号，形成频分复用模拟信号
- 码元传输速率：波特率、**调制速率**
- 奈氏准则：V种码元/信号/幅值-logV，只限制码元传输速率而未限制信息传输速率
  香农：只要实际<极限，就能无差错传输
- 音频信号编码的PCM编码：模拟→数字
- 电气特性：传输速率、距离限制；功能特性：**电平电压意义**
- 同轴电缆、集线器仅半双工，交换机连接可全双工，双绞线可全可半，双绞线连接的速率≥2.5Gbps仅可全双工，光纤仅全双工
- “滑动窗口协议“-GBN/SR
- ==ICMP==
	- 差错报文
		- 源点抑制、终点不可达(区)、时间超过、参数问题、改变路由(重定向)
		- 4种不发送，多播地址、特殊地址
	- 询问报文
		- 回送请求和回答：是否可达，了解状态
		- 时间戳请求和回答：计算RTT
		- 分组网间探测 PING：测试连通性，回送请求和回答，应用层
		- Traceroute：跟踪经过路由，网络层