1. 森林 F 变换为二叉树 B，F 中的每个分支（非终端节点）都在 B 中对应一个右指针为空的结点，F 中最后一颗树的根在 B 中的结点右指针也为空（分支数 + 1）
2. 树 T 的叶节点 → 对应二叉树 B 左指针为空的结点
3. 树 T 的先序遍历与后序遍历分别对应转换后的二叉树 B 的 先序遍历和中序遍历
4. 哈夫曼树高度最高为叶结点数
5. 哈夫曼树的度可以不为2
6. 哈夫曼编码的加权平均长度为$带权长度 WPL / 总频次$
7. 