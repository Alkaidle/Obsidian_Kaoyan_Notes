1. *输入输出*以**文件**为基本单位，*文件存取/传输*的基本单位是**记录**
2. 文件的物理结构中隐式链接的指针存在文件块中，要修改必须磁盘 I/O 读入内存
   索引分配可直接修改已驻留在内存的索引块
3. 索引节点和文件一一对应，索引节点总数即文件总数
4. ⚠区分
	- 磁盘索引节点链接：有多少个**目录项**（文件名）指向这个磁盘索引节点（**硬链接**）
	- 内存索引节点链接：当前有多少个进程正在打开该文件（**引用数**）
	- 链接数=0 且 引用数=0，删除文件数据块