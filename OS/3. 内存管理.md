1. 页表在进程创建时就分配在主存，进程被调度运行时，操作系统把该进程的 **页表基址寄存器（PTBR）** 设置为该进程页表在主存的地址。可以说页表常驻内存能加速虚实地址转换
2. 虚拟存储器==最大容量==由 **CPU 地址位数决定**，和主存、外存无关，==实际可用容量==受 **主存大小 + 外存（交换空间）大小** 限制，如果程序使用的虚拟内存超出主存+外存之和，就会内存不足，导致进程被终止
3. 驻留集可定义为“分配给进程的页框集合“（分配视角）也可以定义为“进程当前实际驻留的页面集合”（实际驻留视角）